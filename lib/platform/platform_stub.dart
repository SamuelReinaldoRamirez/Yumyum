// Ce fichier peut rester vide ou contenir des définitions par défaut si nécessaire.

void redirectToShare(String id) {
  // Version par défaut (mobile)
  throw UnsupportedError('Sharing is not supported on this platform');
}
